diff --git a/live_utilities.vcxproj b/live_utilities.vcxproj
index c8f6827..5631fbe 100644
--- a/live_utilities.vcxproj
+++ b/live_utilities.vcxproj
@@ -132,7 +132,7 @@
       <SDLCheck>true</SDLCheck>
       <PreprocessorDefinitions>NDEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions>
       <ConformanceMode>true</ConformanceMode>
-      <LanguageStandard>stdcpplatest</LanguageStandard>
+      <LanguageStandard>stdcpp20</LanguageStandard>
       <PrecompiledHeader>Use</PrecompiledHeader>
       <PrecompiledHeaderFile>pch/pch.h</PrecompiledHeaderFile>
     </ClCompile>
diff --git a/src/live/CPython.cpp b/src/live/CPython.cpp
index 98889be..f2eb071 100644
--- a/src/live/CPython.cpp
+++ b/src/live/CPython.cpp
@@ -205,14 +205,20 @@ void *CPython::PyImport_GetModuleDict( )
 
 void CPython::AddPythonFunction( void *module, const char *name, void *callback, const char *doc )
 {
-    PyMethodDef *def = new PyMethodDef;
+    // PyCFunction_New keeps a reference to this PyMethodDef for the lifetime of the
+    // resulting callable (it does not copy it), so these must live as long as the
+    // process/module does. A static container with stable references replaces the
+    // previous unmanaged `new PyMethodDef` (no matching delete, and no way to reclaim it).
+    static std::deque< PyMethodDef > s_MethodDefs;
 
-    def->ml_name  = name;
-    def->ml_meth  = callback;
-    def->ml_flags = 0x0001; // METH_VARARGS
-    def->ml_doc   = doc;
+    PyMethodDef &def = s_MethodDefs.emplace_back( );
 
-    void *func = PyCFunction_New( def, nullptr );
+    def.ml_name  = name;
+    def.ml_meth  = callback;
+    def.ml_flags = 0x0001; // METH_VARARGS
+    def.ml_doc   = doc;
+
+    void *func = PyCFunction_New( &def, nullptr );
 
     if ( func )
     {
@@ -256,7 +262,7 @@ int CPython::PyObject_IsTrue( void *obj )
     return Resolve< FN >( "PyObject_IsTrue" )( obj );
 }
 
-void* CPython::PyObject_CallMethodNoArgs( void *self, const char *name )
+void *CPython::PyObject_CallMethodNoArgs( void *self, const char *name )
 {
     using FN = void *( __cdecl * ) ( void *, const char * );
 
@@ -280,7 +286,6 @@ void *CPython::CallStatic( const char *moduleName, const char *objectName, const
     return GetPython( )->PyObject_CallObject( function, nullptr );
 }
 
-
 void *CPython::is_midi_preview( void *self, void *args )
 {
     return GetPython( )->PyBool_FromLong( GetWindow( )->GetMidiPreview( ) );
diff --git a/src/live/CWindow.cpp b/src/live/CWindow.cpp
index 2bdd1f1..d5c0eae 100644
--- a/src/live/CWindow.cpp
+++ b/src/live/CWindow.cpp
@@ -9,7 +9,7 @@ bool CWindow::IsKeyDown( uint8_t iKey )
 
 bool CWindow::IsKeyReleased( uint8_t iKey )
 {
-    static bool bPressed[ 0xFF ];
+    static bool bPressed[ 256 ]; // indexed by uint8_t (0-255); was 0xFF, one short
     if ( !m_bKeyTable[ iKey ] )
     {
         if ( bPressed[ iKey ] )
@@ -23,7 +23,7 @@ bool CWindow::IsKeyReleased( uint8_t iKey )
 
 bool CWindow::IsKeyPressed( uint8_t iKey )
 {
-    static bool bPressed[ 0xFF ];
+    static bool bPressed[ 256 ]; // indexed by uint8_t (0-255); was 0xFF, one short
     if ( m_bKeyTable[ iKey ] )
     {
         if ( !bPressed[ iKey ] )
@@ -105,9 +105,16 @@ LRESULT WINAPI CWindow::GUI_WndProc( HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM
 
     if ( GetWindow( )->IsKeyPressed( 0x35 ) )
     {
+        // These calls end up in raw CPython C-API calls (CLive -> CPython::CallStatic /
+        // PyObject_* ). This runs on the window-procedure thread, which does not hold the
+        // GIL, so it must be acquired explicitly before touching the interpreter.
+        void *gil = GetPython( )->PyGILState_Ensure( );
+
         printf( "GetApplication: 0x%p\n", GetLive( )->GetApplication( ) );
         printf( "GetDocument: 0x%p\n", GetLive( )->GetDocument( ) );
         printf( "IsPlaying: %s\n", GetLive( )->IsPlaying( ) ? "true" : "false" );
+
+        GetPython( )->PyGILState_Release( gil );
     }
 
     return CallWindowProcA( GetWindow( )->m_WndProc, hwnd, uMsg, wParam, lParam );
@@ -301,6 +308,9 @@ bool CWindow::IsSelectedTrack( uint64_t TrackHandle )
 
     auto Track = reinterpret_cast< TPyHandle< ATrack > * >( TrackHandle );
 
+    if ( !Track )
+        return result;
+
     auto *SelectedTrack = Track->Object;
 
     if ( !SelectedTrack )
@@ -376,37 +386,37 @@ void CWindow::OnInit( )
     m_bInit   = true;
 
     {
-        GetPython()->Py_Initialize( );
+        GetPython( )->Py_Initialize( );
         const char *moduleName = "cpp";
         void       *state      = GetPython( )->PyGILState_Ensure( );
         void       *cpp        = GetPython( )->PyModule_New( moduleName );
 
-     if ( cpp )
-     {
-         GetPython( )->AddPythonFunction( cpp, "is_midi_preview", GetPython( )->is_midi_preview, "Check if MIDI Preview is enabled" );
-         GetPython( )->AddPythonFunction( cpp, "send_midi_preview", GetPython( )->send_midi_preview, "Send MIDI Preview" );
-         GetPython( )->AddPythonFunction( cpp, "is_key_released", GetPython( )->is_key_released, "Verify if this key is released" );
-         GetPython( )->AddPythonFunction( cpp, "is_key_pressed", GetPython( )->is_key_pressed, "Verify if this key is pressed" );
-         GetPython( )->AddPythonFunction( cpp, "is_key_down", GetPython( )->is_key_down, "Verify if this key is down" );
-         GetPython( )->AddPythonFunction( cpp, "get_detail_clip", GetPython( )->get_detail_clip, "getting detail clip" );
-         GetPython( )->AddPythonFunction( cpp, "is_selected_track", GetPython( )->is_selected_track, "if the track is selected" );
-         GetPython( )->AddPythonFunction( cpp, "time_selection_start_time", GetPython( )->time_selection_start_time,
-                                          "time_selection_start_time" );
-         GetPython( )->AddPythonFunction( cpp, "time_selection_end_time", GetPython( )->time_selection_end_time,
-                                          "time_selection_end_time" );
-
-     void *modules = GetPython( )->PyImport_GetModuleDict( );
-
-     GetPython( )->PyDict_SetItemString( modules, moduleName, cpp );
-     }
-
-     GetPython( )->PyGILState_Release( state );
-     }
-
-    //AllocConsole( );
-    //FILE *Dummy;
-    //freopen_s( &Dummy, "CONOUT$", "w", stdout );
-    //freopen_s( &Dummy, "CONIN$", "r", stdin );
+        if ( cpp )
+        {
+            GetPython( )->AddPythonFunction( cpp, "is_midi_preview", GetPython( )->is_midi_preview, "Check if MIDI Preview is enabled" );
+            GetPython( )->AddPythonFunction( cpp, "send_midi_preview", GetPython( )->send_midi_preview, "Send MIDI Preview" );
+            GetPython( )->AddPythonFunction( cpp, "is_key_released", GetPython( )->is_key_released, "Verify if this key is released" );
+            GetPython( )->AddPythonFunction( cpp, "is_key_pressed", GetPython( )->is_key_pressed, "Verify if this key is pressed" );
+            GetPython( )->AddPythonFunction( cpp, "is_key_down", GetPython( )->is_key_down, "Verify if this key is down" );
+            GetPython( )->AddPythonFunction( cpp, "get_detail_clip", GetPython( )->get_detail_clip, "getting detail clip" );
+            GetPython( )->AddPythonFunction( cpp, "is_selected_track", GetPython( )->is_selected_track, "if the track is selected" );
+            GetPython( )->AddPythonFunction( cpp, "time_selection_start_time", GetPython( )->time_selection_start_time,
+                                             "time_selection_start_time" );
+            GetPython( )->AddPythonFunction( cpp, "time_selection_end_time", GetPython( )->time_selection_end_time,
+                                             "time_selection_end_time" );
+
+            void *modules = GetPython( )->PyImport_GetModuleDict( );
+
+            GetPython( )->PyDict_SetItemString( modules, moduleName, cpp );
+        }
+
+        GetPython( )->PyGILState_Release( state );
+    }
+
+    // AllocConsole( );
+    // FILE *Dummy;
+    // freopen_s( &Dummy, "CONOUT$", "w", stdout );
+    // freopen_s( &Dummy, "CONIN$", "r", stdin );
 }
 
 CWindow *GetWindow( )
diff --git a/src/live/Python.cpp b/src/live/Python.cpp
deleted file mode 100644
index 2fa94df..0000000
--- a/src/live/Python.cpp
+++ /dev/null
@@ -1,341 +0,0 @@
-#include "pch/pch.h"
-
-template < typename FN >
-FN Resolve( const char *name )
-{
-    static HMODULE module = GetModuleHandleA( LIVE_MODULE );
-
-    return reinterpret_cast< FN >( GetProcAddress( module, name ) );
-}
-
-auto Py_Initialize( ) -> void
-{
-    using FN = void ( * )( );
-
-    Resolve< FN >( "Py_Initialize" )( );
-}
-
-auto PyImport_AddModule( const char *szName ) -> void *
-{
-    using FN = void *( __cdecl * ) ( const char * );
-
-    return Resolve< FN >( "PyImport_AddModule" )( szName );
-}
-
-auto PyImport_ImportModule( const char *szName ) -> void *
-{
-    using FN = void *( __cdecl * ) ( const char * );
-
-    return Resolve< FN >( "PyImport_ImportModule" )( szName );
-}
-
-void *PyObject_GetAttrString( void *obj, const char *attr_name )
-{
-    using FN = void *( __cdecl * ) ( void *, const char * );
-
-    return Resolve< FN >( "PyObject_GetAttrString" )( obj, attr_name );
-}
-
-void *PyUnicode_FromString( const char *str )
-{
-    using FN = void *( __cdecl * ) ( const char * );
-
-    return Resolve< FN >( "PyUnicode_FromString" )( str );
-}
-
-void *PyObject_CallObject( void *callable, void *args )
-{
-    using FN = void *( __cdecl * ) ( void *, void * );
-
-    return Resolve< FN >( "PyObject_CallObject" )( callable, args );
-}
-
-void *PyTuple_New( intptr_t size )
-{
-    using FN = void *( __cdecl * ) ( intptr_t );
-
-    return Resolve< FN >( "PyTuple_New" )( size );
-}
-
-int PyTuple_SetItem( void *tuple, intptr_t index, void *item )
-{
-    using FN = int( __cdecl * )( void *, intptr_t, void * );
-
-    return Resolve< FN >( "PyTuple_SetItem" )( tuple, index, item );
-}
-
-void *PyLong_FromLong( long value )
-{
-    using FN = void *( __cdecl * ) ( long );
-    return Resolve< FN >( "PyLong_FromLong" )( value );
-}
-
-auto PyCallable_Check( void *obj ) -> int
-{
-    using FN = int( __cdecl * )( void * );
-
-    return Resolve< FN >( "PyCallable_Check" )( obj );
-}
-
-auto PyModule_GetDict( void *pModule ) -> void *
-{
-    using FN = void *( __cdecl * ) ( void * );
-
-    return Resolve< FN >( "PyModule_GetDict" )( pModule );
-}
-
-auto PyRun_String( const char *szExec, int start, void *globals, void *locals ) -> void *
-{
-    using FN = void *( __fastcall * ) ( const char *, int, void *, void * );
-
-    return Resolve< FN >( "PyRun_String" )( szExec, start, globals, locals );
-}
-
-auto Py_DecRef( void *pObject ) -> void
-{
-    using FN = void( __cdecl * )( void * );
-
-    return Resolve< FN >( "Py_DecRef" )( pObject );
-}
-
-auto PyGILState_Ensure( ) -> void *
-{
-    using FN = void *( __cdecl * ) ( );
-
-    return Resolve< FN >( "PyGILState_Ensure" )( );
-}
-
-auto PyGILState_Release( void *pObject ) -> void
-{
-    using FN = void( __cdecl * )( void * );
-
-    return Resolve< FN >( "PyGILState_Release" )( pObject );
-}
-
-auto PyErr_Occurred( ) -> void *
-{
-    using FN = void *( __cdecl * ) ( );
-
-    return Resolve< FN >( "PyErr_Occurred" )( );
-}
-
-auto PyErr_Print( ) -> void
-{
-    using FN = void( __cdecl * )( );
-
-    return Resolve< FN >( "PyErr_Print" )( );
-}
-
-auto PyCFunction_New( void *ml, void *self ) -> void *
-{
-    using FN = void *( __cdecl * ) ( void *, void * );
-
-    return Resolve< FN >( "PyCFunction_New" )( ml, self );
-}
-
-auto PyDict_SetItemString( void *p, const char *key, void *val ) -> int
-{
-    using FN = int( __cdecl * )( void *, const char *, void * );
-
-    return Resolve< FN >( "PyDict_SetItemString" )( p, key, val );
-}
-
-auto PyArg_ParseTuple( void *args, const char *format, ... ) -> int
-{
-    using FN = int( __cdecl * )( void *, const char *, va_list );
-
-    auto fn = Resolve< FN >( "PyArg_ParseTuple" );
-
-    va_list va;
-    va_start( va, format );
-
-    int result = fn( args, format, va );
-
-    va_end( va );
-
-    return result;
-}
-
-auto PyArg_VaParse( void *args, const char *format, ... ) -> int
-{
-    using FN = int( __cdecl * )( void *, const char *, va_list );
-
-    auto fn = Resolve< FN >( "PyArg_VaParse" );
-
-    va_list va;
-    va_start( va, format );
-
-    int result = fn( args, format, va );
-
-    va_end( va );
-
-    return result;
-}
-
-auto PyBool_FromLong( long value ) -> void *
-{
-    using FN = void *( __cdecl * ) ( long );
-
-    return Resolve< FN >( "PyBool_FromLong" )( value );
-}
-
-auto PyLong_FromUnsignedLongLong( unsigned long long value ) -> void *
-{
-    using FN = void *( __cdecl * ) ( unsigned long long );
-
-    return Resolve< FN >( "PyLong_FromUnsignedLongLong" )( value );
-}
-
-auto PyFloat_FromDouble( double value ) -> void *
-{
-    using FN = void *( __cdecl * ) ( double );
-
-    return Resolve< FN >( "PyFloat_FromDouble" )( value );
-}
-
-auto PyModule_New( const char *name ) -> void *
-{
-    using FN = void *( __cdecl * ) ( const char * );
-
-    return Resolve< FN >( "PyModule_New" )( name );
-}
-
-void *PyImport_GetModuleDict( )
-{
-    using FN = void *( __cdecl * ) ( );
-
-    return Resolve< FN >( "PyImport_GetModuleDict" )( );
-}
-
-void AddPythonFunction( void *module, const char *name, void *callback, const char *doc )
-{
-    PyMethodDef *def = new PyMethodDef;
-
-    def->ml_name  = name;
-    def->ml_meth  = callback;
-    def->ml_flags = 0x0001; // METH_VARARGS
-    def->ml_doc   = doc;
-
-    void *func = PyCFunction_New( def, nullptr );
-
-    if ( func )
-    {
-        void *dict = PyModule_GetDict( module );
-
-        PyDict_SetItemString( dict, name, func );
-
-        Py_DecRef( func );
-    }
-}
-
-bool ExecutePythonFunction( void *callable, void *args, void **result_out )
-{
-    if ( !callable || !PyCallable_Check( callable ) )
-        return false;
-
-    void* gil = PyGILState_Ensure( );
-
-    void *result = PyObject_CallObject( callable, args );
-
-    if ( !result )
-    {
-        PyErr_Print( );
-        PyGILState_Release( gil );
-        return false;
-    }
-
-    if ( result_out )
-        *result_out = result;
-    else
-        Py_DecRef( result );
-
-    PyGILState_Release( gil );
-    return true;
-}
-
-void *is_midi_preview( void *self, void *args )
-{
-    return PyBool_FromLong( GetWindow( )->GetMidiPreview( ) );
-}
-
-void *send_midi_preview( void *self, void *args )
-{
-    auto  hwnd    = GetWindow( )->GetHWND( );
-    HMENU hMenu   = GetMenu( hwnd );
-    HMENU outMenu = nullptr;
-    UINT  uID     = 0;
-
-    if ( !hMenu )
-        return PyBool_FromLong( 0 );
-
-    if ( GetWindow( )->FindMenuItemID( hMenu, "MIDI Note Editor Preview", uID, outMenu ) )
-        SendMessage( hwnd, WM_COMMAND, uID, 0 );
-
-    return PyBool_FromLong( 0 );
-}
-
-void *is_key_released( void *self, void *args )
-{
-    int iKey = 0;
-    if ( !PyArg_VaParse( args, "i", &iKey ) )
-        return PyBool_FromLong( 0 );
-
-    auto result = GetWindow( )->IsKeyReleased( iKey );
-
-    return PyBool_FromLong( result );
-}
-
-void *is_key_down( void *self, void *args )
-{
-    int iKey = 0;
-    if ( !PyArg_VaParse( args, "i", &iKey ) )
-        return PyBool_FromLong( 0 );
-
-    auto result = GetWindow( )->IsKeyDown( iKey );
-
-    return PyBool_FromLong( result );
-}
-
-void *is_key_pressed( void *self, void *args )
-{
-    int iKey = 0;
-    if ( !PyArg_VaParse( args, "i", &iKey ) )
-        return PyBool_FromLong( 0 );
-
-    auto result = GetWindow( )->IsKeyPressed( iKey );
-
-    return PyBool_FromLong( result );
-}
-
-void *get_detail_clip( void *self, void *args )
-{
-    auto result = GetWindow( )->GetDetailClip( );
-
-    return PyLong_FromUnsignedLongLong( result );
-}
-
-void *is_selected_track( void *self, void *args )
-{
-    bool     result = false;
-    uint64_t track  = 0;
-    if ( !PyArg_VaParse( args, "K", &track ) )
-        return PyBool_FromLong( result );
-
-    result = GetWindow( )->IsSelectedTrack( track );
-
-    return PyBool_FromLong( result );
-}
-
-void *time_selection_start_time( void *self, void *args )
-{
-    auto result = GetWindow( )->StartTime( );
-
-    return PyFloat_FromDouble( result );
-}
-
-void *time_selection_end_time( void *self, void *args )
-{
-    auto result = GetWindow( )->EndTime( );
-
-    return PyFloat_FromDouble( result );
-}
\ No newline at end of file
diff --git a/src/live/Python.h b/src/live/Python.h
deleted file mode 100644
index fb8f971..0000000
--- a/src/live/Python.h
+++ /dev/null
@@ -1,63 +0,0 @@
-#pragma once
-
-template < typename FN >
-FN Resolve( const char* name );
-
-typedef struct
-{
-    const char *ml_name;
-    void       *ml_meth;
-    int         ml_flags;
-    const char *ml_doc;
-} PyMethodDef;
-
-auto Py_Initialize( ) -> void;
-auto PyImport_AddModule( const char *szName ) -> void *;
-auto PyModule_GetDict( void *pModule ) -> void *;
-auto PyRun_String( const char *szExec, int start, void *globals, void *locals ) -> void *;
-auto Py_DecRef( void *pObject ) -> void;
-auto PyGILState_Ensure( ) -> void *;
-auto PyGILState_Release( void *pObject ) -> void;
-auto PyErr_Occurred( ) -> void *;
-auto PyErr_Print( ) -> void;
-auto PyCFunction_New( void *ml, void *self ) -> void *;
-auto PyDict_SetItemString( void *p, const char *key, void *val ) -> int;
-auto PyArg_ParseTuple( void *args, const char *format, ... ) -> int;
-auto PyLong_FromLong( long value ) -> void *;
-auto PyBool_FromLong( long value ) -> void *;
-auto PyLong_FromUnsignedLongLong( unsigned long long value ) -> void *;
-auto PyModule_New( const char *name ) -> void *;
-auto PyImport_GetModuleDict( ) -> void *;
-
-void *is_midi_preview( void *self, void *args );
-void *send_midi_preview( void *self, void *args );
-void *is_key_released( void *self, void *args );
-void *is_key_down( void *self, void *args );
-void *is_key_pressed( void *self, void *args );
-void *get_detail_clip( void *self, void *args );
-void *is_selected_track( void *self, void *args );
-void *time_selection_start_time( void *self, void *args );
-void *time_selection_end_time( void *self, void *args );
-
-void AddPythonFunction( void *module, const char *name, void *callback, const char *doc );
-
-
-
-
-auto PyImport_ImportModule( const char *szName ) -> void *;
-
-void *PyObject_GetAttrString( void *obj, const char *attr_name );
-
-void *PyUnicode_FromString( const char *str );
-
-void *PyObject_CallObject( void *callable, void *args );
-
-void *PyTuple_New( intptr_t size );
-
-int PyTuple_SetItem( void *tuple, intptr_t index, void *item );
-
-void *PyLong_FromLong( long value );
-
-auto PyCallable_Check( void *obj ) -> int;
-
-bool ExecutePythonFunction( void *callable, void *args, void **result_out = nullptr );
\ No newline at end of file
